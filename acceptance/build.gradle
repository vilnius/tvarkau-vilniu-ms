rootProject.dockerCompose.isRequiredBy(test)

test.doFirst {
    rootProject.dockerCompose.exposeAsEnvironment(test)
}

dependencies {
    testCompile 'org.spockframework:spock-core:1.0-groovy-2.4'
}
